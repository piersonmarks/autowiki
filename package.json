{
  "name": "autowiki",
  "version": "0.1.0",
  "private": true,
  "description": "Scaffold an autowiki — an Obsidian knowledge vault with a nightly Claude synthesis job on GitHub Actions.",
  "type": "module",
  "scripts": {
    "init": "bun scripts/init-vault.ts",
    "typecheck": "bun --bun tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
